apiVersion: v1
kind: Pod
metadata:
  name: hostpath-demo
  namespace: default

spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: host-storage
          mountPath: /data

  volumes:
    - name: host-storage
      hostPath:
        path: /home/ubuntu/data
        type: DirectoryOrCreate
